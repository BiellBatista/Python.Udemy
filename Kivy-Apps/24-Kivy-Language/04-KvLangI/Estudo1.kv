#declarando constante, logo abaixo:
#:set x 10

#importação de pacote python (alias pacote), logo abaixo:
#:import o os

Button: #widget principal, porque a identação é zero. Só posso ter um widget princpal
    text: o.getcwd()
